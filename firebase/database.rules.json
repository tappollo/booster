{
  "rules": {
    "userStatus": {
      "$uid": {
        ".write": "$uid === auth.uid",
        ".read": true
      }
    },
    "conversationCounts": {
      "$uid": {
        ".write": "$uid === auth.uid",
        ".read": "$uid === auth.uid"
      }
    },
    "myUnreadCount": {
      "$uid": {
        ".write": "$uid === auth.uid",
        ".read": "$uid === auth.uid"
      }
    }
  }
}
